<div class="flex flex-col items-center justify-center mx-auto min-h-screen pt-12">
  <div class="w-full max-w-2xl px-4 mb-6">
    <h1 class="text-center text-lg font-bold text-gray-600">Editing:
      <%= @movie.title %></h1>
  </div>

  <%= render "form", movie: @movie %>
</div>
