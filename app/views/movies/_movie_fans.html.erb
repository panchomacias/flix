<% if @fans.present? %>
  <div class="w-full py-4">
    <h4 class="text-lg font-semibold text-gray-700 mb-4">
      🎬 Fans (<%= @fans.count %>)
    </h4>
    <div class="flex flex-wrap gap-2 justify-center">
      <% @fans.each do |user| %>
        <%= link_to user.name,
        user_path(user),
        class:
          "inline-flex items-center bg-gradient-to-r from-cyan-50 to-blue-50 
                        text-cyan-700 font-medium px-3 py-2 rounded-full text-sm 
                        border border-cyan-200 hover:from-cyan-100 hover:to-blue-100 
                        hover:border-cyan-300 transition-all duration-200 
                        hover:shadow-sm hover:scale-105" %>
      <% end %>
    </div>
  </div>
<% end %>
